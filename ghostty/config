# The syntax is "key = value". The whitespace around the
# equals doesn't matter.
background = "black"

# Empty values reset the configuration to the default value
font-family = "NvimDark"
font-size = 20

